<% content_for :breadcrumbs do %>
  <%= render 'breadcrumb' %>
<% end %>

<% content_for :body do %>
  <%= render 'body' %>
<% end %>

<%= render 'layouts/crud/show',
item: @measure,
title: "#{@measure.code} #{@measure.name}"
%>